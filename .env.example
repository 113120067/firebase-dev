# Example .env for local development.
# Copy to .env and fill in values, then `npm start`.

# Client-side Firebase config (web app config)
FIREBASE_API_KEY=your-api-key
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_APP_ID=1:1234567890:web:abcdef123456
# Optional:
# FIREBASE_MEASUREMENT_ID=G-XXXXXXX

# Server-side Firebase Admin service account:
# Option A (recommended for container/env-based deploys):
#   Set FIREBASE_SERVICE_ACCOUNT to the base64-encoded JSON of the service account key file.
#   e.g., on mac/linux:
#     export FIREBASE_SERVICE_ACCOUNT=$(base64 /path/to/serviceAccountKey.json)
#
# Option B:
#   Set FIREBASE_SERVICE_ACCOUNT_PATH to the path of the service account JSON file inside the server environment.
#
# Example:
# FIREBASE_SERVICE_ACCOUNT=eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiIs...
# OR
# FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
